---
sidebar_position: 3
---

# cursor

游标（Cursor）是用于执行 SQL 语句和获取结果的主要对象。在向量库的使用场景下，可以通过创建Cursor对结构化的向量数据做增删改查。该使用方式同样适用于关系型数据库的常规DDL DML操作。

## 创建Cursor

## 执行SQL

使用游标的 execute()方法执行 SQL。下面为主要语法：

**execute()主要用于SQL的执行，对于向量库的开发来说，常用语创建Scheme、table，以及插入向量数据等，下面举例：**

- **建表（CREATE）**

- **查询数据 (SELECT):**

使用参数化查询防止SQL注入

**参数化查询 (%s占位符):** 使用 %s作为占位符，防止SQL注入并将实际参数作为元组（或列表）传递给 execute()方法的第二个参数。pymysql会自动处理转义和类型转换。

- **插入数据 (INSERT):**

- **更新数据 (UPDATE):**

- **删除数据 (DELETE):**

## 处理结果

主要用于查询后获取输出结果，进而进行处理

- **获取所有结果 (fetchall()):**

- **获取下一行结果 (fetchone()):**

- **获取指定数量的结果 (fetchmany(size)):**

## 关闭连接

操作完成后，务必关闭游标和连接以释放资源。